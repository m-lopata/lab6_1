name: Build APK

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        # Update system and install necessary packages
        sudo apt-get update
        sudo apt-get install -y openjdk-8-jdk wget unzip libffi-dev python3-venv

        # Download Android SDK Command-Line Tools
        mkdir -p $HOME/android-sdk/cmdline-tools
        cd $HOME/android-sdk/cmdline-tools
        wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O tools.zip
        unzip tools.zip
        mv cmdline-tools tools
        cd ~

        # Set environment variables
        echo "ANDROID_SDK_ROOT=$HOME/android-sdk" >> $GITHUB_ENV
        echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
        echo "PATH=$PATH:$HOME/android-sdk/cmdline-tools/tools/bin:$HOME/android-sdk/platform-tools" >> $GITHUB_ENV

        # Install necessary Android SDK components
        yes | $HOME/android-sdk/cmdline-tools/tools/bin/sdkmanager --licenses
        $HOME/android-sdk/cmdline-tools/tools/bin/sdkmanager \
          "platform-tools" \
          "build-tools;33.0.0" \
          "platforms;android-33"

        # Create old-style path for Buildozer compatibility
        mkdir -p $HOME/.buildozer/android/platform/android-sdk/tools/bin
        ln -s $HOME/android-sdk/cmdline-tools/tools/bin/sdkmanager $HOME/.buildozer/android/platform/android-sdk/tools/bin/sdkmanager

        # Ensure build-tools is in the expected location
        mkdir -p $HOME/.buildozer/android/platform/android-sdk/build-tools
        cp -r $HOME/android-sdk/build-tools/33.0.0 $HOME/.buildozer/android/platform/android-sdk/build-tools/

        # Install Python dependencies
        python -m pip install --upgrade pip
        pip install buildozer cython

    - name: Build APK
      run: |
        buildozer -v android debug

    - name: Upload APK artifact
      uses: actions/upload-artifact@v3
      with:
        name: apk
        path: ./bin/*.apk
